$(document).ready(function(){
    //Add Class to header when the page is scrolled 
    $(window).scroll(function(){
        if($(window).scrollTop() > 0){
            $('header').addClass('scrolled');
        }
        else{
            $('header').removeClass('scrolled');
        }
    });
    //Change icon of hamburger menu 
    $('button.navbar-toggler').click(function(){
        $(this).find('i').toggleClass('fa-bars fa-times');
    });

    //This handles smooth page scrolling, you need to add class of smoothScroll to the a tag
    $('a.smoothScroll[href*="#"]')
        // Remove links that don't actually link to anything
        .not('[href="#"]')
        .not('[href="#0"]')
        .click(function(event) {
            // On-page links
            if (
                location.pathname.replace(/^\//, "") ==
                this.pathname.replace(/^\//, "") &&
                location.hostname == this.hostname
            ) {
                // Figure out element to scroll to
            var target = $(this.hash);
            target = target.length
                ? target
                : $("[name=" + this.hash.slice(1) + "]");
            // Does a scroll target exist?
            if (target.length) {
                // Only prevent default if animation is actually going to happen
                event.preventDefault();
                $("html, body").animate(
                    {
                        scrollTop: target.offset().top - 100
                    },
                    1000,
                    function() {
                        // Callback after animation
                        // Must change focus!
                        var $target = $(target);
                        $target.focus();
                        if ($target.is(":focus")) {
                            // Checking if the target was focused
                            return false;
                        } else {
                            $target.attr("tabindex", "-1"); // Adding tabindex for elements not focusable
                            $target.focus(); // Set focus again
                        }
                    }
                );
            }
        }
    });

    //Owl Carousel Placeholder item for displaying single item
    /* $('.classHere').owlCarousel({
        autoplay: true,
        autoplayHoverPause: false,
        dots: true,
        loop: true,
        nav: false,
        pagination: true,
        animateOut: 'slideOutUp',
        animateIn: 'slideInUp',
        paginationSpeed: 4500,
        singleItem: false,
        slideSpeed: 6500,
        items: 1
    }); */

    //Owl Carousel placeholder item for displaying multiple slide items at one
    /* $('.classHere').owlCarousel({
        autoplay: true,
        center: true,
        dots: true,
        nav: true,
        loop: true,
        slideSpeed: 3500,
        margin: 30,
        navText: [
            '<i class="fas fa-chevron-left"></i>',
            '<i class="fas fa-chevron-right"></i>'
        ],
        responsive: {
            0: {
                items: 1
            },
            700: {
                items: 1
            },
            1100: {
                items: 2
            }
        }
    }); */
});